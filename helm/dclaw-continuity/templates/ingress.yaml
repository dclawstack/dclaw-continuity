apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-continuity.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-continuity.name" . }}
spec:
  rules:
    - host: dclaw-continuity.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-continuity.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
